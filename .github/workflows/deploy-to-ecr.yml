name: Build and Deploy to Agent Core

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: diary-orchestrator-agent
  IMAGE_TAG: ${{ github.sha }}

jobs:
  build-and-deploy:
    name: Build ECR Image and Deploy to Agent Core
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Create ECR repository if not exists
      run: |
        aws ecr describe-repositories --repository-names ${{ env.ECR_REPOSITORY }} || \
        aws ecr create-repository --repository-name ${{ env.ECR_REPOSITORY }} \
          --image-scanning-configuration scanOnPush=true \
          --region ${{ env.AWS_REGION }}

    - name: Build, tag, and push image to Amazon ECR
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      run: |
        echo "🔨 Docker 이미지 빌드 중 (ARM64 아키텍처, 캐시 비활성화 - 항상 새로 빌드)..."
        docker buildx create --use --name arm64-builder || docker buildx use arm64-builder
        docker buildx build --platform linux/arm64 \
          --no-cache \
          -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \
          -t $ECR_REGISTRY/$ECR_REPOSITORY:latest \
          --push .
        
        echo "✅ ECR 이미지 푸시 완료 (ARM64, 캐시 없이 새로 빌드됨)"
        echo "Image URI: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install boto3

    - name: Deploy to Agent Core Runtime
      run: |
        echo "🚀 Agent Core Runtime 배포 시작..."
        python deploy_from_ecr.py

    - name: Deployment complete
      run: |
        echo "::notice title=배포 완료::ECR 이미지 빌드 및 Agent Core Runtime 배포 완료 (tag: ${{ env.IMAGE_TAG }})"

